# =============================================================================
# requirements.txt
# Lista de dependencias del proyecto MGTA_reforma.
#
# Cómo instalar todo de una vez:
#   pip install -r requirements.txt
# =============================================================================

# Manipulación y análisis de datos tabulares (DataFrames)
pandas>=2.0.0

# Operaciones matemáticas y arrays numéricos (np.where, np.select...)
numpy>=1.24.0

# Generación de gráficos (diagramas de Newell, balance de capacidad, etc.)
matplotlib>=3.7.0

# Heatmaps del análisis de sensibilidad (construido sobre matplotlib)
seaborn>=0.12.0

# Exportación de archivos Excel con múltiples pestañas (.xlsx)
openpyxl>=3.1.0

# Solver de Programación Lineal Entera — equivalente a intlinprog de MATLAB
pulp>=2.7.0

# Tests automatizados
pytest>=7.0.0

# Solver de Programación Lineal Entera — equivalente a intlinprog de MATLAB
pulp>=2.9.0

# Libreria para trabajar con linear programing en python (más rápida)
scipy>=1.10.0